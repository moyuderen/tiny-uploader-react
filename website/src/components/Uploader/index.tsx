import { Uploader, UploadIcon, type UploaderHandle, CheckStatus, type FileContext } from '@tinyuploader/react';
import '@tinyuploader/react/style.css'

export default Uploader

export { UploadIcon, type UploaderHandle, CheckStatus, type FileContext }